<merge xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Không có container nào ở đây, chỉ có nội dung -->
    <ImageView android:src="@mipmap/ic_launcher" android:layout_width="50dp" android:layout_height="50dp"/>
    <TextView android:text="User (Merge)" android:layout_gravity="center" android:layout_marginLeft="10dp" android:layout_width="wrap_content" android:layout_height="wrap_content"/>
</merge>